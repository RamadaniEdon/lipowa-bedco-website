import { cn } from "@/lib/utils";
import type { ClassValue } from "clsx";

export default function ContactForm({ className }: { className?: ClassValue }) {
    return <div className={cn(className)}>asdf</div>;
}
